import React from 'react';

const Dog = ({dog}) => {
    return (
        <div>
            <div>{dog}</div>
            <button>delete</button>

            
        </div>
    );
};

export default Dog;